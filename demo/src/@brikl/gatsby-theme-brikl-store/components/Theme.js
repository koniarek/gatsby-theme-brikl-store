import { theme as baseTheme } from "@brikl/gatsby-theme-brikl-store/src/components/Theme"

export const theme = {
  ...baseTheme,
  palette: {
    ...baseTheme.palette,
    primary: {
      main: "#d4d4d4",
    },
  },
}
